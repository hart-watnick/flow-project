"use strict";

$(document).ready(() =>{
    $("#DealerId, #SourceId, #SourceTypeId, #LeadDesignationId, #RouteInformationId, #CrmId, #LeadTypeId").select2({
        placeholder: "Search..",
        allowClear: true,
        width: "66%",
    });

    
});